#!/bin/sh
set -eu

mkdir -p "$(dirname "${DATABASE_PATH:-/data/module_o_mat.db}")" \
  "${MANUAL_UPLOADS_DIR:-/data/uploads/manuals}"

cd -P -- "$(dirname -- "$0")"
./migrate
PHX_SERVER=true exec ./module_o_mat start
